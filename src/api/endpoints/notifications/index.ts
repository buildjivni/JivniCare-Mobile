/**
 * Placeholder — Section 7 (Endpoint Organization). No endpoint functions are implemented in
 * this milestone. Future home for F17 (In-App Notifications) — `GET /api/notifications`,
 * `PATCH /api/notifications/mark-read`, `GET /api/notifications/unread-count` — built against
 * `docs/11-API-Contract.md`'s F17 section once repositories/services are in scope.
 */
export {};
