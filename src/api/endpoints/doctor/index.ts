/**
 * Placeholder — Section 7 (Endpoint Organization). No endpoint functions are implemented in
 * this milestone. Future home for F02/F03 (Doctor Search + Filters) — built against
 * `docs/11-API-Contract.md`'s F02/F03 section (`GET`/`POST /api/v1/patient/search`, also flat
 * JSON, not enveloped) once repositories/services are in scope.
 */
export {};
