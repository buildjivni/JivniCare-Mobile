/**
 * Placeholder — Section 7 (Endpoint Organization). No endpoint functions are implemented in
 * this milestone. Future home for F01 (Phone OTP Login/Signup) — `sendOtp()`, `verifyOtp()`,
 * `refresh()` — built against `docs/11-API-Contract.md`'s F01 section once repositories/
 * services are in scope. Note for that future work: F01's `send-otp`/`verify-otp`/`refresh`
 * responses are flat JSON, not the `{success,data}` envelope (API Contract's Response Envelope
 * section).
 */
export {};
