/**
 * Placeholder — Section 7 (Endpoint Organization). No endpoint functions are implemented in
 * this milestone. Unlike the other five domains, `docs/11-API-Contract.md` does not document a
 * patient profile-edit endpoint at all — flagged here rather than guessed at; whoever builds
 * this domain next needs that endpoint documented (or confirmed as another backend gap, per the
 * contract's own F19 precedent) before writing anything beyond this placeholder.
 */
export {};
